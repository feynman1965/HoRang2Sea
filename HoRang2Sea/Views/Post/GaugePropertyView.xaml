<UserControl x:Class="HoRang2Sea.Views.GaugePropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:local="clr-namespace:HoRang2Sea.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label  Grid.Column="0" Grid.Row="0"
                Content="Global Items" Foreground="Gray" FontSize="14"/>

        <dxe:ListBoxEdit ItemsSource="{Binding GaugeGlobalItems}" Grid.Column="0" Grid.Row="1" Grid.RowSpan="3"  Margin="5"
           dx:ListBoxDragDropBehavior.AllowDragDrop="True"
           dx:ListBoxDragDropBehavior.DragRecordOver="OnDragRecordOver" />

        <Label  Grid.Column="1" Grid.Row="0"
                Content="Gauge Item" Foreground="Gray" FontSize="14"/>

        <dxe:ListBoxEdit ItemsSource="{Binding GaugeItems}" Grid.Column="1" Grid.Row="1" Grid.RowSpan="1"  Margin="5"
           dx:ListBoxDragDropBehavior.AllowDragDrop="True"
           dx:ListBoxDragDropBehavior.DragRecordOver="OnYDragRecordOver" />

        <StackPanel Grid.Column="1" Grid.Row="3" Grid.RowSpan="1"  Margin="5">
            
            <StackPanel Orientation="Horizontal" Margin="10">
                <Label Content="Start Value"></Label>
                <dxe:TextEdit Name="StartEdit" Width="100" Text="{Binding StartValue}" Margin="10,0,0,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="10">
                <Label Content="End Value"></Label>
                <dxe:TextEdit Name="EndEdit" Width="100" Text="{Binding EndValue}" Margin="13,0,0,0"/>
            </StackPanel>

        </StackPanel>

    </Grid>
</UserControl>

