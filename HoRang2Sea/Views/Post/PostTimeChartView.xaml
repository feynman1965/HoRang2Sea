<UserControl x:Class="HoRang2Sea.Views.PostTimeChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HoRang2Sea.Views"
             xmlns:ext="http://schemas.abtsoftware.co.uk/scichart/exampleExternals"
              xmlns:s="http://schemas.abtsoftware.co.uk/scichart"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <DataTemplate x:Key="TimeChartPropertyDialogTemplate">
            <local:TimeChartPropertyView/>
        </DataTemplate>
    </UserControl.Resources>

    <dxmvvm:Interaction.Behaviors>
        <dx:DialogService x:Name="ChartPropertyDialogService" ViewTemplate="{StaticResource TimeChartPropertyDialogTemplate}">
            <dx:DialogService.DialogStyle>
                <Style TargetType="Window">
                    <Setter Property="Width" Value="600" />
                    <Setter Property="MaxWidth" Value="1000" />
                    <Setter Property="WindowStyle" Value="ToolWindow" />
                </Style>
            </dx:DialogService.DialogStyle>
        </dx:DialogService>
    </dxmvvm:Interaction.Behaviors>


    <dxb:BarManager>
        <dxb:BarManager.Items>
            <dxb:BarButtonItem Name="bChartProperties" CategoryName="Solution" Glyph="{dx:DXImage 'SvgImages/DiagramIcons/PropertiesPanel.svg'}" Hint="Chart Property" Command="{Binding OnChartPropertyCommand}" />
        </dxb:BarManager.Items>

        <dxb:BarManager.Bars>
            <dxb:Bar Caption="Chart" x:Name="Chart" UseWholeRow="True" AllowQuickCustomization="False" ShowDragWidget="False">
                <dxb:Bar.DockInfo>
                    <dxb:BarDockInfo ContainerName="ChartBar" />
                </dxb:Bar.DockInfo>
                <dxb:Bar.ItemLinks>
                    <dxb:BarButtonItemLink BarItemName="bChartProperties"/>
                </dxb:Bar.ItemLinks>
            </dxb:Bar>
        </dxb:BarManager.Bars>

        <Grid>
            <!--<Grid.RowDefinitions>
                <RowDefinition Height="35" />
                <RowDefinition />
            </Grid.RowDefinitions>-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--<dxb:BarContainerControl x:Name="ChartBar" Width="35" HorizontalAlignment="Left" Margin="0,5,0,0" Grid.Row="0"/>-->

            <StackPanel Orientation="Vertical">
                <Button Style="{StaticResource DefaultButtonStyle}" ToolTip="Properties" Command="{Binding OnChartPropertyCommand}" Width="21" Height="21">
                    <ext:ToolbarIcon Width="13" Kind="ViewDashboardOutline" Margin="-10" Style="{StaticResource ToolbarButtonIconStyle}"/>
                </Button>
            </StackPanel>
                <!--<ext:SciChartInteractionToolbar  x:Name="UserToolbar" TargetSurface="{Binding Source={x:Reference Name=sciChart}}">
                <Button Style="{StaticResource DefaultButtonStyle}" ToolTip="Properties" Command="{Binding OnChartPropertyCommand}">
                    <ext:ToolbarIcon Kind="ViewDashboardOutline" Margin="-7" Style="{StaticResource ToolbarButtonIconStyle}"/>
                </Button>
            </ext:SciChartInteractionToolbar>-->
            <s:SciChartSurface x:Name="sciChart" Grid.Column="1"
                                   RenderableSeries="{s:SeriesBinding RenderableSeries}"
                                   ChartTitle="Monitor Chart"
                                   s:VisualXcceleratorEngine.IsEnabled="True"
                                   XAxes= "{s:AxesBinding XAxes}"
                                   YAxes="{s:AxesBinding YAxes}"
                                   Background="White" >

                <!--<XAxes "{s:AxesBinding XAxes}"-->
                <!--YAxes="{s:AxesBinding YAxes}"-->

                <s:SciChartSurface.ChartModifier>
                    <s:ModifierGroup>
                        <s:RubberBandXyZoomModifier ExecuteOn="MouseLeftButton"
                                                 RubberBandFill="#33FFFFFF" RubberBandStroke="#AAFFFFFF"
                                                 RubberBandStrokeDashArray="2 2"/>
                        <s:ZoomPanModifier ExecuteOn="MouseRightButton" ClipModeX="None" />
                        <!--<s:XAxisDragModifier DragMode="Pan" ToolBar.OverflowMode="Never"/>
                        <s:MouseWheelZoomModifier/>
                        <s:ZoomExtentsModifier ExecuteOn="MouseDoubleClick" />-->
                        <s:LegendModifier ShowLegend="True" Orientation="Horizontal"
                                       VerticalAlignment="Bottom"                                     
                                       HorizontalAlignment="Center"
                                       LegendPlacement="Inside" ToolBar.OverflowMode="Never"/>
                        <s:RolloverModifier ExecuteOn="MouseMove" ShowTooltipOn="MouseHover" />
                    </s:ModifierGroup>
                </s:SciChartSurface.ChartModifier>
            </s:SciChartSurface>


        </Grid>
    </dxb:BarManager>
</UserControl>

