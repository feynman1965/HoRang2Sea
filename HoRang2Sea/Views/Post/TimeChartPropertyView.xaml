<UserControl x:Class="HoRang2Sea.Views.TimeChartPropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:local="clr-namespace:HoRang2Sea.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Global Items 레이블 -->
        <Label  Grid.Column="0" Grid.Row="0"
                Content="Global Items" Foreground="Gray" FontSize="14"/>

        <!-- 검색어 입력 -->
        <dxe:TextEdit Grid.Column="0" Grid.Row="1" Margin="5" Height="25"
                      Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                      NullText="Search Global Items..." />

        <!-- Global Items 리스트 -->
        <dxe:ListBoxEdit ItemsSource="{Binding FilteredChartGlobalItems}" 
                         Grid.Column="0" Grid.Row="2" Grid.RowSpan="3"  Margin="5"
                         dx:ListBoxDragDropBehavior.AllowDragDrop="True"
                         dx:ListBoxDragDropBehavior.DragRecordOver="OnDragRecordOver" />

        <!-- Y Axis Item 레이블 -->
        <Label  Grid.Column="1" Grid.Row="0"
                Content="Y Axis Item" Foreground="Gray" FontSize="14"/>

        <!-- Y Axis Items 리스트 -->
        <dxe:ListBoxEdit ItemsSource="{Binding ChartYItems}" 
                         Grid.Column="1" Grid.Row="1" Grid.RowSpan="4"  Margin="5"
                         dx:ListBoxDragDropBehavior.AllowDragDrop="True"
                         dx:ListBoxDragDropBehavior.DragRecordOver="OnYDragRecordOver" />

    </Grid>
</UserControl>
