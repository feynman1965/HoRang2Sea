<UserControl x:Class="HoRang2Sea.Views.SolutionExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HoRang2Sea.Views" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" 
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <dx:BackgroundPanel>
            <DockPanel>
                <dxb:BarContainerControl x:Name="SolutionBar" DockPanel.Dock="Top" />
                <Border>
                    <dxg:TreeViewControl x:Name="treeView"
                                         ItemsSource="{Binding Solution.Items}"
                                         SelectedItem="{Binding SelectedItem}"
                                         ChildNodesPath="Items"
                                         ShowBorder="False"
                                         ShowSearchPanel="True"
                                         dx:ScrollBarExtensions.ScrollBarMode="TouchOverlap"
                                         ShowCheckboxes="False"
                                         AllowRecursiveNodeChecking="True"
                                         ShowNodeImages="True"
                                         AutoExpandAllNodes="False"
                                         TreeDerivationMode="ChildNodesSelector"
                                         TreeViewFieldName="Name"
                                         AllowEditing="True"
                                         AllowDragDrop="False"
                                         NodeDoubleClick="treeview_NodeDoubleClick"
                                         IsNodeMenuEnabled="True"
                                         SortMode="DisplayText"
                                         ShowingEditor="treeview_ShowingEditor"
                                         >
                    <dxg:TreeViewControl.NodeMenuCustomizations>
                        <dxb:BarButtonItem Name="deleteNode" Content="Delete Node" Command="{DXBinding '@a($UserControl).DataContext.OnDeleteNodeCommand'}"/>
                    </dxg:TreeViewControl.NodeMenuCustomizations>

                    <dxg:TreeViewControl.NodeImageSelector>
                            <local:ReportLibraryNodeImageSelector/>
                        </dxg:TreeViewControl.NodeImageSelector>
                    </dxg:TreeViewControl>
                </Border>
            </DockPanel>
    </dx:BackgroundPanel>
</UserControl>
